---

# 🎲 真心话大冒险 Online (Truth or Dare Online)

一个基于 Web 的实时多人在线“真心话大冒险”派对游戏。支持创建房间、实时聊天、管理员后台管理、题库自定义及上帝视角监控等功能。采用暗黑霓虹风格设计，完美适配移动端与桌面端。

---

## ✨ 主要功能 (Features)

### 🎮 玩家端

* **实时互动**：基于 Socket.io 的低延迟通信，游戏状态秒级同步。
* **房间系统**：
* 支持创建公开房间或带密码的私密房间。
* 多维度题库选择（可多选不同分类）。


* **游戏流程**：
* **随机选人**：房主控制转盘随机抽取一名“幸运”玩家。
* **双模式抽取**：玩家可选择“真心话”或“大冒险”，且支持按难度等级（Lv.1 - Lv.5）随机出题。
* **卡片展示**：精美的题目展示动画。


* **即时通讯**：房间内支持实时弹幕式聊天。
* **题目贡献**：玩家可在前台提交新的创意题目。

### 🛡️ 管理员后台

* **仪表盘**：实时监控活跃房间、在线人数及题库统计。
* **上帝视角 (Spectator Mode)**：管理员可隐身进入任意房间旁观，不干扰游戏进程。
* **题库管理**：
* **全功能编辑**：修改题目内容、分类、类型及难度等级。
* **批量导入**：支持粘贴多行文本快速扩充题库。
* **分类管理**：增删改查题目分类。


* **审核机制**：玩家提交的题目默认进入审核队列，管理员批准后方可上线。
* **数据安全**：支持软删除（回收站）和硬删除（彻底粉碎），以及误删恢复功能。

---

## 🛠 技术栈 (Tech Stack)

### 前端 (Client)

* **框架**: Vue 3 (Composition API)
* **构建工具**: Vite
* **路由**: Vue Router 4
* **样式**: Tailwind CSS (Utility-first CSS framework)
* **通信**: Socket.io-client, Axios

### 后端 (Server)

* **运行时**: Node.js
* **框架**: Express
* **通信**: Socket.io
* **ORM**: Prisma
* **数据库**: SQLite (默认) / 可无缝切换 PostgreSQL 或 MySQL

---

## 🚀 快速开始 (Getting Started)

### 1. 环境要求

* Node.js >= 16.0.0
* npm 或 pnpm

### 2. 克隆项目

```bash
git clone https://github.com/yourusername/truth-or-dare-online.git
cd truth-or-dare-online

```

### 3. 后端设置 (Server)

进入 server 目录并安装依赖：

```bash
cd server
npm install

```

配置环境变量：
复制 `.env.example` (如果没有则新建 `.env`) 并填入以下内容：

```properties
PORT=3000
# 前端运行地址，用于 CORS 允许跨域
CLIENT_URL=http://localhost:5173
# 管理员登录密钥
ADMIN_PASSWORD=admin666
# 数据库连接 (默认 SQLite)
DATABASE_URL="file:./dev.db"

```

初始化数据库：

```bash
npx prisma migrate dev --name init

```

启动后端服务：

```bash
npm run dev

```

> 后端将在 `http://localhost:3000` 启动。

### 4. 前端设置 (Client)

新开一个终端窗口，进入 client 目录：

```bash
cd client
npm install

```

配置环境变量：
在 `client` 根目录下新建 `.env` 文件：

```properties
# 后端 API 地址
VITE_API_BASE_URL=http://localhost:3000

```

启动前端开发服务器：

```bash
npm run dev

```

> 前端将在 `http://localhost:5173` 启动。

---

## 📂 项目结构 (Project Structure)

```
truth-or-dare-online/
├── client/                 # 前端 Vue 项目
│   ├── public/             # 静态资源 (favicon, logo)
│   ├── src/
│   │   ├── api.js          # Axios 封装
│   │   ├── socket.js       # Socket.io 封装
│   │   ├── views/          # 页面组件 (Home, GameRoom, Admin, Submit)
│   │   └── router/         # 路由配置
│   ├── .env                # 前端环境变量
│   └── vite.config.js      # Vite 配置
│
├── server/                 # 后端 Node 项目
│   ├── prisma/             # 数据库模型与迁移
│   │   └── schema.prisma   # 数据表定义
│   ├── index.js            # 服务端入口 (Express + Socket)
│   └── .env                # 后端环境变量
│
└── README.md

```

---

## 📖 管理员指南 (Admin Guide)

1. **访问入口**：在浏览器访问 `http://localhost:5173/admin`。
2. **身份验证**：输入 `.env` 中配置的 `ADMIN_PASSWORD` (默认为 `admin666`)。
3. **上帝视角**：
* 在“仪表盘”页面，点击活跃房间卡片上的 **“👁️ 上帝视角”** 按钮。
* 将打开新窗口进入房间，您将处于隐身状态，底部操作栏被禁用。


4. **导入题目**：
* 进入“题库管理” -> 点击“📥 导入”。
* 选择分类、类型和等级，粘贴文本（每行一题），点击确认即可批量添加。



---

## 🤝 贡献 (Contributing)

欢迎提交 Issue 或 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

---

## 📄 开源协议 (License)

本项目采用 [MIT](https://www.google.com/search?q=LICENSE) 协议开源。

---

**Have fun & Don't lie! 🤫**